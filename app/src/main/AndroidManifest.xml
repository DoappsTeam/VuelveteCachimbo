<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.doapps.vuelvetecahimbo" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MaterialTheme" >
        <activity
            android:name="me.doapps.vuelvetecachimbo.activity.SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="me.doapps.vuelvetecachimbo.activity.MainActivity"></activity>
        <activity android:name="me.doapps.vuelvetecachimbo.activity.UniversityActivity"></activity>
        <activity android:name="me.doapps.vuelvetecachimbo.activity.FilterActivity"></activity>
    </application>

</manifest>
